apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: replit-control-plane
spec:
  selector:
    app: orchestrator
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4100
  type: ClusterIP
