apiVersion: v1
kind: Pod
metadata:
  name: s3-test
  namespace: replit
spec:
  serviceAccountName: repl-workload
  containers:
    - name: aws-cli
      image: amazon/aws-cli:latest
      command:
        - sleep
        - "3600"